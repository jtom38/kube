---
apiVersion: v1
kind: Service

metadata:
  name: nc-web-access

spec:
  selector:
    app: nc-web 
 
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: web-access

